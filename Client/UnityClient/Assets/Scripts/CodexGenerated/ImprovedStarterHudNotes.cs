using UnityEngine;

public sealed class ImprovedStarterHudNotes : MonoBehaviour
{
    private void Start()
    {
        Debug.Log("Improved starter HUD pass loaded: quest tracker, status panel, minimap, and action hints are present.");
    }
}
